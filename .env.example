# Local-only. No DATABASE_URL exists by design (SQLite file backend).
GROQ_API_KEY=
ZEN_API_KEY=
JOIN_CODE_PEPPER=
ADMIN_CODE=

# --- Codex primary (GPT-5.6 Luna) — all optional; game falls back to Groq/Zen when unset ---
# CODEX_ENABLED=true
# CODEX_BIN=codex
# CODEX_HOME=  # dedicated dir for Codex auth (must persist across restarts to stay logged in)
# CODEX_RUNTIME_DIR=  # isolated cwd for Codex turns (defaults to OS temp redline-codex-runtime)
# CODEX_MAX_CONCURRENCY=11
# CODEX_RPC_TIMEOUT_MS=30000
# CODEX_FIRST_ACTIVITY_TIMEOUT_MS=45000
# CODEX_TURN_TIMEOUT_MS=90000
