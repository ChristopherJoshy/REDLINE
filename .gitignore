# Deps & build output
node_modules/
dist/
*.tsbuildinfo

# Runtime SQLite — live event data (sessions, scores). Schema re-seeds from backend/src/db/schema.sql
*.db
*.db-*
*.sqlite
*.sqlite-*
/data/
backend/data/
backend/backups/

# Secrets — never committed (.env.example documents the shape)
.env
.env.*
!.env.example
*.local

# Codex App Server auth + isolated runtime (may contain account credentials)
.codex-home/
backend/data/codex-runtime/
.codex/

# Hosting / logs / coverage
.vercel/
*.log
logs/
coverage/
deploy-vps.sh

# OS noise
.DS_Store
Thumbs.db
*.tar.gz
